# pi-websearch

Web search tools for pi, backed by independent engines (mwmbl + Mojeek) instead
of a shared SearXNG instance.

Provides three tools: `web_search`, `fetch_content`, and `get_search_results`.
Drop-in replacement for `pi-searxng`.

For the system specification, see [SPEC.md](SPEC.md).
